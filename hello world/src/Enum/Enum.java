package Enum;

public enum Enum {
COREJAVA(1),COLLECTIONS(2),GENERICS(3),JSPANDSERVLETS(4),MUILTITHREADING(5);
	
	public int value;
 
	private Enum(int value) {
		this.value = value;
	}
	
	
}